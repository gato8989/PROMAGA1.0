{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "HEROKU_BUILDPACK_PHP"
  },
  "deploy": {
    "startCommand": "php artisan migrate --seed && php artisan serve",
    "restartPolicyType": "ON_FAILURE"
  }
}